# تشغيل PivotScope خارج Arena — الأسهم الأمريكية

## المتطلبات

- كمبيوتر أو خادم عليه **Node.js 20 أو أحدث**.
- اتصال إنترنت من الجهاز الذي سيشغّل التطبيق. التطبيق يجلب الأسعار من Yahoo Finance، بلا مفتاح API.
- السماح باتصالات HTTPS الصادرة إلى `query1.finance.yahoo.com` على المنفذ `443`.

> إذا لم يسمح مزوّد الاستضافة باتصال الخادم بهذا العنوان، لن تصل الأسعار الحقيقية. لا يكفي اتصال الإنترنت على جهاز المتصفح؛ الاتصال المطلوب من الجهاز الذي يشغّل التطبيق.

## على Windows

1. نزّل Node.js LTS من `https://nodejs.org` وثبّته.
2. فك ضغط ملف `PivotScope-Live-US.zip`.
3. افتح المجلد الذي يحتوي `package.json`، ثم افتح PowerShell داخله.
4. نفّذ الأوامر التالية:

```powershell
npm install
Copy-Item .env.example .env.local
npm run db:migrate
npm run dev
```

5. افتح `http://localhost:3000`.
6. اختر الاستراتيجية، اكتب رمز السهم الأمريكي مثل `AAPL`، اترك وضع Demo مغلقاً، ثم اضغط **تشغيل التحليل**.

## تشغيل نسخة الإنتاج

بعد تثبيت الحزم وتهيئة قاعدة البيانات:

```powershell
npm run build
npm run start
```

ثم افتح `http://localhost:3000`.

## ما الذي يجب أن تراه؟

- الوضع الافتراضي **Live**؛ لا يستبدل التطبيق الرمز تلقائياً برمز تجريبي.
- إذا كان Yahoo متاحاً، تظهر الشموع والتحليل ومصدر البيانات `yahoo`.
- إذا تعذر الاتصال، تظهر رسالة خطأ واضحة. لا تُعرض بيانات Demo بدلاً من الأسعار الحقيقية.
- لا يوجد مفتاح API داخل الملف ولا تحتاج مفتاحاً لمصدر Yahoo الحالي.

## إذا ظهرت رسالة تعذر الاتصال

جرّب من PowerShell على نفس الجهاز الذي يشغّل التطبيق:

```powershell
Invoke-WebRequest "https://query1.finance.yahoo.com/v8/finance/chart/AAPL?interval=1d&range=5d"
```

إذا فشل الاختبار، فالشبكة أو جدار الحماية يمنع الاتصال. اسمح باتصال HTTPS إلى `query1.finance.yahoo.com:443`، أو شغّل التطبيق على جهاز/استضافة تسمح بالاتصال الخارجي. لا تضع أي كلمة سر أو مفتاح في المحادثة.

للتجربة البصرية فقط، فعّل مربع Demo في التطبيق؛ هذا الوضع يستخدم أسعاراً مصطنعة وليس للتداول.
